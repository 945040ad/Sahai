version: 1
frontend:
  phases:
    build:
      commands:
        - echo "Static site — no build step"
  artifacts:
    baseDirectory: govfixer-frontend
    files:
      - '**/*'
  cache:
    paths: []
